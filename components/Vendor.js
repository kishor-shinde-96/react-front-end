import React from 'react';

const Vendor = () => {
    return (
        <div>
            <h2>Vendor Screen</h2>
            {/* Vendor screen content */}
        </div>
    );
};

export default Vendor;
